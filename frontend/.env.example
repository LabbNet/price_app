VITE_API_URL=http://localhost:4000

# Optional: Google Places Autocomplete for account addresses.
# Create a browser-restricted key at console.cloud.google.com with
# Maps JavaScript API + Places API enabled. Without a key, the
# form still works (plain text input, no suggestions).
VITE_GOOGLE_MAPS_API_KEY=
