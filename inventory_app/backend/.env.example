# Server
PORT=4100
NODE_ENV=development

# CORS — comma-separated origins, or leave unset to allow all in dev
CORS_ORIGIN=http://localhost:5273

# SQLite database file (relative to backend/)
DATABASE_FILE=./data/inventory.sqlite
